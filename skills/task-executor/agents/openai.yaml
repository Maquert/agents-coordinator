interface:
  display_name: "Task Executor"
  short_description: "Execute and finish single or batched backlog tasks"
  default_prompt: "Use $task-executor to execute, validate, merge, and clean up a backlog task or ordered batch."
