interface:
  display_name: "Task Executor"
  short_description: "Execute one pending or WIP backlog task end-to-end"
  default_prompt: "Use $backlog-task-execution to pick and finish one backlog task safely."
