interface:
  display_name: "Backlog Task Intake"
  short_description: "Capture prompts as normalized pending tasks"
  default_prompt: "Use $task-processor to capture this New task: request without implementing it."
